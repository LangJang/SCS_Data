# _incoming — 待整理脚本暂存区

将你写好的脚本文件（`.py`、`.ipynb` 等）放入此文件夹，我会：

1. **审查** — 理解脚本的功能和逻辑
2. **整理** — 提取核心逻辑，拆分 UI / 数据处理 / 导出
3. **标准化** — 统一命名、类型标注、docstring 格式
4. **集成** — 放入 `src/ui/` 或 `src/core/` 对应位置

放入后告诉我一声即可（例如："incoming 里有新脚本了"）。
